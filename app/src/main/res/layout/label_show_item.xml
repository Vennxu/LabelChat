<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/label_show_right_leaf">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:gravity="center_horizontal|bottom"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/label"
            style="@style/LabelShowTextStyle"
            android:layout_weight="1"
            android:background="@null"
            android:ellipsize="end"
            android:text="@string/label_sample_0" />

        <Space
            android:layout_width="5dp"
            android:layout_height="0dp" />

        <TextView
            android:id="@+id/praise"
            style="@style/LabelShowTextStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:drawableEnd="@drawable/ic_label_show_praise"
            android:drawableRight="@drawable/ic_label_show_praise"
            android:gravity="bottom|center_horizontal"
            android:singleLine="true"
            android:text="@string/label_count_sample"
            android:textColor="#FFA5D8D1"
            android:textSize="13sp" />
    </LinearLayout>
</RelativeLayout>